# ── Web Framework ─────────────────────────────────────────────────────────
fastapi
uvicorn
starlette
python-multipart

# ── Database ──────────────────────────────────────────────────────────────
SQLAlchemy
alembic

# ── Templating ────────────────────────────────────────────────────────────
Jinja2

# ── Auth & Security ───────────────────────────────────────────────────────
python-jose[cryptography]
passlib[bcrypt]
bcrypt
cryptography

# ── Config & Validation ───────────────────────────────────────────────────
pydantic
pydantic-settings

# ── HTTP Client (AI integrations) ─────────────────────────────────────────
httpx
anyio

# ── Communication (optional) ──────────────────────────────────────────────
# Install twilio only if WhatsApp sending is needed.
# twilio>=8.0.0

# ── AI / LLM (optional) ───────────────────────────────────────────────────
# Install anthropic to enable real LLM responses in the Zia assistant.
# Without it, Zia falls back to keyword-only responses — fully functional.
# anthropic>=0.25.0
