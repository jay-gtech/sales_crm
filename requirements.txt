# ── Web Framework ─────────────────────────────────────────────────────────
fastapi
uvicorn
starlette
python-multipart

# ── Database ──────────────────────────────────────────────────────────────
SQLAlchemy
alembic

# ── Templating ────────────────────────────────────────────────────────────
Jinja2

# ── Auth & Security ───────────────────────────────────────────────────────
python-jose[cryptography]
passlib[bcrypt]
bcrypt==3.2.0
cryptography

# ── Config & Validation ───────────────────────────────────────────────────
pydantic
pydantic-settings

# ── HTTP Client (AI integrations) ─────────────────────────────────────────
httpx
anyio

# ── Communication (optional) ──────────────────────────────────────────────
# Install twilio only if WhatsApp sending is needed.
# twilio>=8.0.0

# ── AI / LLM (optional) ───────────────────────────────────────────────────
# Install groq to enable real LLM responses in the Zia assistant.
# Without it, Zia falls back to keyword-only responses — fully functional.
groq>=0.5.0
